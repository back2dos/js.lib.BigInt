# js.lib.BigInt

Externs for JavaScript's builtin BigInt

This should probably at some point be added to stdlib (and probably also be used to back haxe.Int64 on JS), but until such time, this library will fill the gap.